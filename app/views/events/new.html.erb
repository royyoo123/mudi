<%= simple_form_for @event do |f| %>
	<%= f.input :name %>
	<%= f.input :price %>
	<%= f.input :address %>
	<%= f.input :date %>
	<%= f.input :description %>
	<%= f.input :time %>
	<%= f.input :photos, as: :file, input_html: { multiple: true } %>
	<%= f.submit %>
<% end %>